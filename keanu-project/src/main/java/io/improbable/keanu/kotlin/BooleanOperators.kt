package io.improbable.keanu.kotlin

interface BooleanOperators<T> {
    // operator fun not() : T
    // TODO: hmmm, Kotlin doesn't seem to allow override of Boolean operators
    // TODO: what a shame. Need to decide on a standard way of doing boolean ops
}